import type { ReactNode } from "react";
import type { Item } from "../types";

export function CardList({}: { items: Item[], children?: ReactNode }) {
    return (
        <>
            This is a card list I promise
        </>
    );
}